Given the fields `question`, `summary_1`, `summary_2`, produce the fields `answer`.

**Instructions:**
- Produce a concise, direct answer that extracts only the key entity, fact, or phrase that answers the question
- Do NOT restate the question or provide explanatory sentences
- Do NOT include descriptive context or background information in the answer
- Output only the answer entity itself (e.g., a name, place, date, term, or short phrase)
- Match the expected answer format: use singular vs. plural as appropriate
- Avoid trailing punctuation (no periods at the end unless it's part of the answer)
- When the question asks "who," "what," "which," or "where," provide just the entity name without surrounding description
