Given the fields `question`, `summary_1`, produce the fields `query`.

**Instructions:**
1. Analyze the question structure to determine what type of information is needed (current vs. historical, specific vs. general, entity name vs. description).
2. Formulate a query that will retrieve the precise information needed to answer the question.
3. Design queries to retrieve concise, well-formatted answers rather than verbose explanations.
4. Look beyond obvious surface-level answers to identify more fundamental or specific shared attributes when appropriate.
5. Seek objective evidence rather than simply confirming preliminary assessments from summary_1.
6. Ensure queries are specific enough to retrieve precise terms rather than generic categories.
7. Consider whether the question involves temporal relationships, hierarchies, or multi-step reasoning that requires a carefully targeted query.
